// The single source of truth for the site's absolute origin. Every SEO
// surface (metadataBase, canonical, sitemap, robots, JSON-LD) derives its
// URLs from this constant; nothing else may hardcode a domain.
//
// PLACEHOLDER DOMAIN, PENDING APPROVAL. No production domain has been
// approved yet. Change this one line when the real domain lands.
export const SITE_URL = "https://gallium-supply.vercel.app";
